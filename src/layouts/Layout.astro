<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta
      name="description"
      content="Dilan Grijalva is a software developer in Managua, Nicaragua focused on mobile apps, backend systems, and reliable product interfaces."
    />
    <meta name="theme-color" content="#FBFAF7" />
    <link type="image/png" sizes="32x32" rel="icon" href="/favicon.png" />
    <meta name="generator" content={Astro.generator} />
    <title>Dilan Grijalva | Software Developer</title>
  </head>
  <body>
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --color-background: #fbfaf7;
    --color-surface: #ffffff;
    --color-text: #111418;
    --color-muted: #6f6f68;
    --color-border: #d9d7d0;
    --color-accent: #ff4f00;
    --color-accent-soft: #fff0e8;
    --font-sans: ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont,
      "Segoe UI", sans-serif;
    --font-mono: "SFMono-Regular", "Roboto Mono", "JetBrains Mono", Consolas,
      monospace;
    background: var(--color-background);
    color: var(--color-text);
    font-family: var(--font-sans);
    font-synthesis-weight: none;
    text-rendering: optimizeLegibility;
  }

  * {
    box-sizing: border-box;
  }

  html,
  body {
    margin: 0;
    min-width: 320px;
    min-height: 100%;
  }

  html {
    scroll-behavior: smooth;
  }

  body {
    background:
      linear-gradient(rgba(17, 20, 24, 0.025) 1px, transparent 1px) 0 0 /
        100% 96px,
      var(--color-background);
    color: var(--color-text);
  }

  a {
    color: inherit;
  }

  img {
    display: block;
    max-width: 100%;
  }

  ::selection {
    background: var(--color-accent);
    color: var(--color-text);
  }

  :focus-visible {
    outline: 2px solid var(--color-accent);
    outline-offset: 4px;
  }

  @media (prefers-reduced-motion: reduce) {
    html {
      scroll-behavior: auto;
    }

    *,
    *::before,
    *::after {
      animation-duration: 0.001ms !important;
      animation-iteration-count: 1 !important;
      scroll-behavior: auto !important;
      transition-duration: 0.001ms !important;
    }
  }
</style>
